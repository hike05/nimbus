# Быстрая настройка VPN

## Текущий статус
✅ Сервер обновлен  
✅ Админ-панель работает: https://vov.sneakernet.top/api/v2/storage/login  
✅ Логин: `admin` / Пароль: `admin2024`

---

## Шаг 1: Создать пользователя (через админ-панель)

1. Откройте https://vov.sneakernet.top/api/v2/storage/login
2. Войдите с учетными данными выше
3. Нажмите "Add User"
4. Введите имя пользователя (например, `testuser`)
5. Нажмите "Create"

Система автоматически создаст конфигурации для всех VPN протоколов.

---

## Шаг 2: Запустить VPN сервисы

```bash
ssh mq@138.124.101.56
cd /home/mq/nimbus

# Проверить, что конфигурации созданы
ls -la data/stealth-vpn/configs/

# Запустить VPN сервисы
docker compose up -d xray trojan singbox

# Проверить статус
docker compose ps
```

---

## Шаг 3: Проверить логи

```bash
# Xray
docker compose logs xray --tail=30

# Trojan
docker compose logs trojan --tail=30

# Sing-box
docker compose logs singbox --tail=30
```

**Ожидаемый результат:** Сервисы должны запуститься без ошибок и показать "Up (healthy)"

---

## Шаг 4: Скачать клиентские конфигурации

В админ-панели:
1. Перейдите на страницу пользователя
2. Скачайте конфигурации для нужных протоколов:
   - Xray (VLESS)
   - Trojan
   - Sing-box

---

## Шаг 5: Подключиться к VPN

### Для Xray (рекомендуется)
1. Установите клиент: v2rayN (Windows), v2rayNG (Android), Qv2ray (Linux/Mac)
2. Импортируйте конфигурацию
3. Подключитесь

### Для Trojan
1. Установите клиент: Clash, ClashX, Clash for Android
2. Импортируйте конфигурацию
3. Подключитесь

---

## Если что-то не работает

### VPN сервис не запускается

```bash
# Проверить логи
docker logs stealth-xray --tail=50

# Проверить конфигурацию
docker exec stealth-xray cat /etc/xray/xray.json

# Перезапустить
docker compose restart xray
```

### Нет конфигураций

Убедитесь, что пользователь создан через админ-панель. Проверьте:

```bash
cat data/stealth-vpn/configs/users.json
ls -la data/stealth-vpn/configs/clients/
```

### Сертификаты не найдены

```bash
# Проверить сертификаты
ls -la data/caddy/certificates/acme-v02.api.letsencrypt.org-directory/vov.sneakernet.top/

# Если пусто, скопировать из volume Caddy
docker run --rm -v stealth-caddy-data:/from -v /home/mq/nimbus/data/caddy/certificates:/to alpine sh -c 'cp -r /from/caddy/certificates/* /to/'
```

---

## WireGuard (опционально)

WireGuard требует дополнительной настройки:

```bash
# Исправить права
docker run --rm -v stealth-wireguard-data:/data alpine chmod -R 777 /data

# Запустить
docker compose up -d wireguard

# Проверить
docker compose logs wireguard --tail=30
```

---

## Полезные команды

```bash
# Статус всех сервисов
docker compose ps

# Логи в реальном времени
docker compose logs -f

# Перезапуск конкретного сервиса
docker compose restart xray

# Остановить все VPN сервисы
docker compose stop xray trojan singbox wireguard

# Запустить все VPN сервисы
docker compose up -d xray trojan singbox wireguard
```

---

## Готово! 🎉

После выполнения этих шагов у вас будет:
- ✅ Работающий VPN сервер
- ✅ Несколько протоколов на выбор
- ✅ Админ-панель для управления
- ✅ SSL сертификаты
