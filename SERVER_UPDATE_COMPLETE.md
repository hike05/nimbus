# Обновление сервера завершено

**Дата:** 18 ноября 2025  
**Сервер:** vov.sneakernet.top (138.124.101.56)  
**Статус:** ✅ Частично работает

---

## ✅ Что работает

### 1. Caddy (Reverse Proxy)
- **Статус:** ✅ Работает
- **SSL:** ✅ Сертификаты Let's Encrypt активны
- **Порты:** 80, 443 (HTTP/HTTPS)
- **Проверка:** `curl -k https://vov.sneakernet.top`

### 2. Админ-панель
- **Статус:** ✅ Работает
- **URL:** https://vov.sneakernet.top/api/v2/storage/login
- **Логин:** `admin`
- **Пароль:** `admin2024`
- **Функции:** Управление пользователями, просмотр логов

### 3. SSL Сертификаты
- **Статус:** ✅ Сохранены
- **Расположение:** `/home/mq/nimbus/data/caddy/certificates/`
- **Домен:** vov.sneakernet.top
- **Срок действия:** Автоматическое обновление через Caddy

---

## ⚠️ Что требует настройки

### VPN Сервисы (остановлены)
- **Xray** - нет конфигурации
- **Trojan** - нет конфигурации  
- **Sing-box** - нет конфигурации
- **WireGuard** - проблемы с правами

**Причина:** Отсутствуют конфигурационные файлы для VPN протоколов

---

## 🔧 Исправленные проблемы

### 1. CPU Limits
- **Было:** `cpus: '2.0'` (сервер имеет только 1 CPU)
- **Стало:** `cpus: '0.8'`
- **Файл:** `docker-compose.yml`

### 2. Caddyfile Syntax
- **Было:** `auto_https`, `tls sni` (несовместимо с Caddy v2)
- **Стало:** Минимальная рабочая конфигурация
- **Файл:** `config/Caddyfile`

### 3. Пути к сертификатам
- **Было:** `/etc/letsencrypt/live/${DOMAIN}/fullchain.pem`
- **Стало:** `/etc/letsencrypt/acme-v02.api.letsencrypt.org-directory/${DOMAIN}/${DOMAIN}.crt`
- **Файлы:** `xray/entrypoint.sh`, `trojan/entrypoint.sh`, `singbox/entrypoint.sh`

### 4. User Permissions
- **Было:** `user: "1000:1000"` в docker-compose.yml
- **Стало:** Удалено (запуск от root)
- **Причина:** Проблемы с правами на существующие volumes

### 5. Admin Panel Password
- **Было:** Хеш bcrypt с $ символами, которые интерпретировались docker-compose
- **Стало:** Запуск контейнера напрямую с переменными окружения
- **Пароль:** `admin2024`

### 6. Docker Network
- **Проблема:** Caddy не мог найти контейнер admin через DNS
- **Решение:** Добавлен network alias `admin`

---

## 📋 Следующие шаги

### Шаг 1: Создать пользователя VPN через админ-панель

1. Откройте https://vov.sneakernet.top/api/v2/storage/login
2. Войдите (admin / admin2024)
3. Создайте нового пользователя
4. Система автоматически сгенерирует конфигурации для всех VPN протоколов

### Шаг 2: Проверить созданные конфигурации

```bash
ssh mq@138.124.101.56
cd /home/mq/nimbus
ls -la data/stealth-vpn/configs/
```

Должны появиться файлы:
- `xray.json`
- `trojan.json`
- `singbox.json`
- `wireguard/wg0.conf`

### Шаг 3: Запустить VPN сервисы

```bash
cd /home/mq/nimbus
docker compose up -d xray trojan singbox
```

### Шаг 4: Проверить статус

```bash
docker ps
docker compose logs xray --tail=20
docker compose logs trojan --tail=20
docker compose logs singbox --tail=20
```

### Шаг 5: Исправить WireGuard (если нужен)

WireGuard требует дополнительной настройки прав:

```bash
# Исправить права на volume
docker run --rm -v stealth-wireguard-data:/data alpine chmod -R 777 /data

# Запустить WireGuard
docker compose up -d wireguard
```

---

## 🔒 Безопасность

### Изменить пароль админа

После первого входа рекомендуется изменить пароль:

```bash
ssh mq@138.124.101.56
cd /home/mq/nimbus
python3 admin-panel/setup_admin.py ВАШ_НОВЫЙ_ПАРОЛЬ
```

Скопируйте новый хеш и обновите контейнер:

```bash
docker stop stealth-admin
docker rm stealth-admin
docker run -d --name stealth-admin --network stealth-vpn \
  --network-alias admin \
  -v /home/mq/nimbus/data/stealth-vpn:/data/stealth-vpn \
  -v /home/mq/nimbus/data/caddy/certificates:/etc/letsencrypt:ro \
  -e ADMIN_USERNAME=admin \
  -e ADMIN_PASSWORD_HASH='НОВЫЙ_ХЕШ' \
  -e SESSION_SECRET=15e244fd01a26010711ffa67e329c1f26e18d8daae7b7f0c7c5fadbc7d57d5b3 \
  -e DOMAIN=vov.sneakernet.top \
  stealth-admin:latest
```

---

## 📊 Текущая конфигурация

### Запущенные контейнеры
```
stealth-caddy       - Reverse proxy (порты 80, 443)
stealth-admin       - Админ-панель (порт 8010, внутренний)
```

### Остановленные контейнеры
```
stealth-xray        - Xray VPN (ждет конфигурации)
stealth-trojan      - Trojan VPN (ждет конфигурации)
stealth-singbox     - Sing-box VPN (ждет конфигурации)
stealth-wireguard   - WireGuard VPN (проблемы с правами)
```

### Volumes
```
stealth-caddy-data       - SSL сертификаты и данные Caddy
stealth-admin-data       - Данные админ-панели
stealth-xray-data        - Данные Xray
stealth-trojan-data      - Данные Trojan
stealth-singbox-data     - Данные Sing-box
stealth-wireguard-data   - Данные WireGuard
```

---

## 🐛 Известные проблемы

### 1. Docker Compose и bcrypt хеши
**Проблема:** Docker Compose интерпретирует `$` в .env файле как переменные  
**Решение:** Запуск admin контейнера напрямую с `-e` флагами

### 2. WireGuard права доступа
**Проблема:** Не может создать директории в /config  
**Решение:** Запуск от root или исправление прав на volume

### 3. Отсутствие конфигураций VPN
**Проблема:** Сервисы не могут запуститься без конфигов  
**Решение:** Создать пользователя через админ-панель

---

## 📝 Полезные команды

### Проверка статуса
```bash
docker ps
docker compose ps
docker compose logs -f
```

### Перезапуск сервисов
```bash
docker compose restart caddy
docker compose restart admin
```

### Просмотр логов
```bash
docker logs stealth-caddy --tail=50
docker logs stealth-admin --tail=50
```

### Проверка сертификатов
```bash
docker exec stealth-caddy ls -la /data/caddy/certificates/acme-v02.api.letsencrypt.org-directory/vov.sneakernet.top/
```

### Проверка сети
```bash
docker network inspect stealth-vpn
```

---

## ✅ Критерии успеха

- [x] Caddy работает с SSL
- [x] Админ-панель доступна
- [x] Сертификаты сохранены
- [ ] Создан хотя бы один VPN пользователь
- [ ] VPN сервисы запущены и работают
- [ ] VPN подключение протестировано

---

## 🎯 Итог

**Основная инфраструктура работает:**
- ✅ HTTPS с Let's Encrypt
- ✅ Админ-панель доступна
- ✅ Данные и сертификаты сохранены

**Для полной работы VPN нужно:**
1. Создать пользователя через админ-панель
2. Запустить VPN сервисы
3. Протестировать подключение

**Время на завершение:** ~10-15 минут
